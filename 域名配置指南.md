# MultiAgent 域名配置指南

## 方案对比

| 方案 | 成本 | 难度 | 需要公网IP | 推荐度 |
|------|------|------|-----------|--------|
| Cloudflare Tunnel | 免费 | ⭐ | ❌ | ⭐⭐⭐⭐⭐ |
| Ngrok | 免费/付费 | ⭐ | ❌ | ⭐⭐⭐⭐ |
| 云服务器+域名 | ¥50-200/年 | ⭐⭐⭐ | ❌ | ⭐⭐⭐ |
| 本地Nginx+域名 | ¥30-60/年 | ⭐⭐⭐⭐ | ✅ | ⭐⭐ |

---

## 方案一：Cloudflare Tunnel（强烈推荐）

### 优点
- ✅ 完全免费
- ✅ 无需公网IP
- ✅ 自动HTTPS
- ✅ 自带DDoS防护
- ✅ 配置简单

### 步骤

#### 1. 注册Cloudflare账号
访问 https://dash.cloudflare.com 注册账号

#### 2. 购买域名并添加到Cloudflare
- 在Cloudflare添加你的域名
- 按照提示修改域名DNS服务器

#### 3. 安装cloudflared
```bash
# Windows使用winget安装
winget install Cloudflare.cloudflared

# 或下载: https://developers.cloudflare.com/cloudflare-one/connections/connect-networks/downloads/
```

#### 4. 创建Tunnel
```bash
# 登录
cloudflared tunnel login

# 创建tunnel
cloudflared tunnel create multiagent

# 配置路由
cloudflared tunnel route dns multiagent yourdomain.com

# 运行tunnel（将5001端口暴露到域名）
cloudflared tunnel run --url http://localhost:5001 multiagent
```

#### 5. 访问
```
https://yourdomain.com
```

---

## 方案二：使用Ngrok（快速测试）

### 步骤

#### 1. 注册Ngrok
访问 https://ngrok.com 注册账号

#### 2. 下载并安装
```bash
# 下载: https://ngrok.com/download
# 解压到项目目录
```

#### 3. 配置Authtoken
```bash
ngrok config add-authtoken YOUR_TOKEN
```

#### 4. 启动
```bash
# 将5001端口暴露到公网
ngrok http 5001
```

#### 5. 访问
Ngrok会给你一个临时域名，例如：
```
https://abc123.ngrok-free.app
```

---

## 方案三：云服务器 + 域名（生产环境）

### 需要的资源
1. 云服务器（阿里云/腾讯云/AWS）
2. 已备案的域名

### 步骤

#### 1. 购买云服务器
- 阿里云 ECS: https://www.aliyun.com
- 腾讯云 CVM: https://cloud.tencent.com

#### 2. 安装Nginx
```bash
# Ubuntu/Debian
sudo apt update
sudo apt install nginx -y

# CentOS/RHEL
sudo yum install nginx -y
```

#### 3. 配置Nginx反向代理
```nginx
server {
    listen 80;
    server_name yourdomain.com www.yourdomain.com;
    
    location / {
        proxy_pass http://你的本地IP:5001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

#### 4. 配置SSL证书（Let's Encrypt）
```bash
# 安装certbot
sudo apt install certbot python3-certbot-nginx -y

# 自动配置SSL
sudo certbot --nginx -d yourdomain.com -d www.yourdomain.com
```

#### 5. 访问
```
https://yourdomain.com
```

---

## 方案四：本地Nginx + 域名（需要公网IP）

### 前提条件
- 有公网IP
- 域名已解析到你的公网IP
- 路由器已配置端口转发（80→你的电脑，443→你的电脑）

### 步骤

#### 1. 安装Nginx for Windows
```bash
# 下载: http://nginx.org/en/download.html
# 解压到 D:\Python_project\MultiAgent\nginx
```

#### 2. 配置Nginx
编辑 `nginx/conf/nginx.conf`，参考项目中的 `nginx/nginx.conf` 文件

#### 3. 配置SSL证书
```bash
# 使用Let's Encrypt（需要certbot）
# 或使用已有的 cert.pem 和 key.pem
```

#### 4. 启动Nginx
```bash
cd nginx
nginx.exe
```

---

## 快速开始推荐

### 对于个人项目：使用 Cloudflare Tunnel
```bash
# 1. 安装
winget install Cloudflare.cloudflared

# 2. 运行（替换为你的域名）
cloudflared tunnel --url http://localhost:5001

# 3. 访问生成的临时域名
```

### 对于测试演示：使用 Ngrok
```bash
# 1. 下载 ngrok
# 2. 运行
ngrok http 5001

# 3. 访问显示的HTTPS链接
```

---

## 注意事项

1. **File System Access API限制**
   - 即使在域名下，也需要HTTPS才能使用
   - Cloudflare Tunnel和Ngrok都自动提供HTTPS

2. **性能考虑**
   - 内网穿透会有一定延迟
   - 生产环境建议使用云服务器

3. **安全性**
   - 不要将敏感项目暴露到公网
   - 使用强密码和访问控制

4. **防火墙**
   - 确保5001端口在本地防火墙开放
   - Windows Defender防火墙添加入站规则

---

## 常见问题

### Q: 我没有公网IP怎么办？
A: 使用Cloudflare Tunnel或Ngrok，无需公网IP

### Q: 域名需要备案吗？
A: 
- 国内服务器：需要备案
- Cloudflare/ Ngrok：不需要备案

### Q: 免费方案有限制吗？
A: 
- Cloudflare Tunnel：无限制
- Ngrok免费版：有带宽和连接数限制

### Q: 如何保证安全？
A: 
- 使用HTTPS（所有方案都支持）
- 添加身份验证
- 限制访问IP