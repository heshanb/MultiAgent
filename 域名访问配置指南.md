# MultiAgent 项目 - 域名访问配置指南

> 📅 创建时间：2026-07-25
> 📝 状态：待配置

---

## 📋 目录

- [项目概述](#项目概述)
- [当前状态](#当前状态)
- [方案对比](#方案对比)
- [方案一：Cloudflare Tunnel（推荐）](#方案一cloudflare-tunnel推荐)
- [方案二：Ngrok（快速测试）](#方案二ngrok快速测试)
- [方案三：云服务器 + 域名（生产环境）](#方案三云服务器--域名生产环境)
- [方案四：本地Nginx + 域名](#方案四本地nginx--域名)
- [准备工作](#准备工作)
- [常见问题](#常见问题)
- [相关文件](#相关文件)

---

## 项目概述

MultiAgent项目是一个基于FastAPI + Uvicorn的Python Web应用，提供：
- 代码编辑器（支持项目导入、文件树、代码高亮）
- Python代码运行环境
- AI聊天功能

**当前访问方式：**
- HTTP: `http://localhost:5001`
- HTTPS: `https://localhost:5001`（已配置SSL证书）

**目标：**
实现通过域名访问，例如 `https://www.yourdomain.com`，让所有IP的用户都能正常使用所有功能。

---

## 当前状态

### ✅ 已完成
- [x] SSL证书生成（自签名证书）
  - 证书文件：`cert.pem`
  - 私钥文件：`key.pem`
  - 有效期：10年（到2036年）
- [x] main.py支持HTTPS自动检测
- [x] 监听 `0.0.0.0:5001`（允许所有IP访问）
- [x] Ngrok已下载（位于项目根目录）
- [x] Nginx配置文件模板（位于 `nginx/` 目录）

### ⏳ 待完成
- [ ] 选择并配置域名访问方案
- [ ] 购买域名（如未购买）
- [ ] 配置DNS解析
- [ ] 配置SSL证书（Let's Encrypt或其他）
- [ ] 配置反向代理

---

## 方案对比

| 方案 | 成本 | 难度 | 需要公网IP | 需要域名 | 稳定性 | 推荐度 | 适用场景 |
|------|------|------|-----------|---------|--------|--------|----------|
| **Cloudflare Tunnel** | 免费 | ⭐ | ❌ | ✅ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | 个人项目、长期演示 |
| **Ngrok** | 免费/付费 | ⭐ | ❌ | ❌ | ⭐⭐⭐ | ⭐⭐⭐⭐ | 临时演示、测试 |
| **云服务器+域名** | ¥50-200/年 | ⭐⭐⭐ | ❌ | ✅ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | 生产环境、正式项目 |
| **本地Nginx+域名** | ¥30-60/年 | ⭐⭐⭐⭐ | ✅ | ✅ | ⭐⭐⭐⭐ | ⭐⭐ | 有公网IP的家庭宽带 |

---

## 方案一：Cloudflare Tunnel（推荐）

### 优点
- ✅ 完全免费
- ✅ 自动HTTPS
- ✅ 无需公网IP
- ✅ 自带DDoS防护
- ✅ 配置简单
- ✅ 域名固定（不会变化）

### 缺点
- ❌ 需要购买域名
- ❌ 需要修改域名DNS服务器

### 详细步骤

#### 1. 注册Cloudflare账号
访问 https://dash.cloudflare.com 注册账号

#### 2. 购买域名
推荐注册商：
- 阿里云：https://wanwang.aliyun.com（¥30-60/年）
- 腾讯云：https://dnspod.cloud.tencent.com（¥30-60/年）
- Namecheap：https://www.namecheap.com（$8-10/年）

#### 3. 添加域名到Cloudflare
1. 登录Cloudflare Dashboard
2. 点击 "Add a Site"
3. 输入你的域名
4. 按照提示修改域名DNS服务器为Cloudflare提供的地址

#### 4. 安装cloudflared
```bash
# Windows使用winget安装
winget install Cloudflare.cloudflared

# 或手动下载: https://developers.cloudflare.com/cloudflare-one/connections/connect-networks/downloads/
```

#### 5. 创建Tunnel
```bash
# 登录Cloudflare
cloudflared tunnel login

# 创建tunnel
cloudflared tunnel create multiagent

# 配置DNS路由
cloudflared tunnel route dns multiagent yourdomain.com

# 运行tunnel（将5001端口暴露到域名）
cloudflared tunnel run --url http://localhost:5001 multiagent
```

#### 6. 访问
```
https://yourdomain.com
```

### 一键配置脚本
项目根目录提供了 `setup-domain.bat`，选择选项1即可自动引导配置。

---

## 方案二：Ngrok（快速测试）

### 优点
- ✅ 快速上手（5分钟搞定）
- ✅ 无需域名
- ✅ 无需公网IP
- ✅ 自动HTTPS
- ✅ 免费版可用

### 缺点
- ❌ 每次重启链接会变化
- ❌ 免费版有带宽限制（约40GB/月）
- ❌ 首次访问需要点击"Visit Site"按钮
- ❌ 不适合长期使用

### 详细步骤

#### 1. 注册Ngrok
访问 https://dashboard.ngrok.com/signup 注册账号

#### 2. 获取Authtoken
登录后访问：https://dashboard.ngrok.com/get-started/your-authtoken
复制你的Authtoken

#### 3. 配置Authtoken
```bash
cd D:\Python_project\MultiAgent
.\ngrok.exe config add-authtoken YOUR_TOKEN
```

#### 4. 启动Python服务
```bash
python main.py
```

#### 5. 启动Ngrok（新窗口）
```bash
cd D:\Python_project\MultiAgent
.\ngrok.exe http 5001
```

#### 6. 获取访问链接
Ngrok会显示类似：
```
Forwarding  https://abc123def456.ngrok-free.app -> http://localhost:5001
```

### 当前状态
- ✅ Ngrok已下载并解压到项目根目录
- ⏳ 待配置Authtoken
- ⏳ 待启动测试

### 快速启动脚本
```batch
@echo off
cd /d D:\Python_project\MultiAgent
echo 正在启动Ngrok...
ngrok.exe http 5001
pause
```

---

## 方案三：云服务器 + 域名（生产环境）

### 优点
- ✅ 最稳定
- ✅ 完全控制
- ✅ 适合生产环境
- ✅ 可自定义配置

### 缺点
- ❌ 需要付费（¥50-200/年）
- ❌ 配置复杂
- ❌ 国内服务器需要备案

### 需要的资源
1. 云服务器（阿里云/腾讯云/AWS）
2. 已备案的域名（国内服务器）

### 详细步骤

#### 1. 购买云服务器
- 阿里云 ECS: https://www.aliyun.com
- 腾讯云 CVM: https://cloud.tencent.com
- AWS EC2: https://aws.amazon.com

#### 2. 安装Nginx
```bash
# Ubuntu/Debian
sudo apt update
sudo apt install nginx -y

# CentOS/RHEL
sudo yum install nginx -y
```

#### 3. 配置Nginx反向代理
创建配置文件 `/etc/nginx/sites-available/multiagent`：

```nginx
server {
    listen 80;
    server_name yourdomain.com www.yourdomain.com;
    
    location / {
        proxy_pass http://127.0.0.1:5001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # WebSocket支持
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}
```

启用配置：
```bash
sudo ln -s /etc/nginx/sites-available/multiagent /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx
```

#### 4. 配置SSL证书（Let's Encrypt）
```bash
# 安装certbot
sudo apt install certbot python3-certbot-nginx -y

# 自动配置SSL
sudo certbot --nginx -d yourdomain.com -d www.yourdomain.com
```

#### 5. 访问
```
https://yourdomain.com
```

---

## 方案四：本地Nginx + 域名

### 前提条件
- ✅ 有公网IP
- ✅ 域名已解析到你的公网IP
- ✅ 路由器已配置端口转发（80→你的电脑，443→你的电脑）

### 优点
- ✅ 成本低
- ✅ 完全控制
- ✅ 适合家庭服务器

### 缺点
- ❌ 需要公网IP
- ❌ 需要配置路由器端口转发
- ❌ 家庭宽带IP可能变化

### 详细步骤

#### 1. 安装Nginx for Windows
下载地址：http://nginx.org/en/download.html
解压到：`D:\Python_project\MultiAgent\nginx`

#### 2. 配置Nginx
编辑 `nginx/conf/nginx.conf`，参考项目中的 `nginx/nginx.conf` 文件

关键配置：
```nginx
server {
    listen 443 ssl;
    server_name yourdomain.com;
    
    ssl_certificate     cert/cert.pem;
    ssl_certificate_key cert/key.pem;
    
    location / {
        proxy_pass http://127.0.0.1:5001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

#### 3. 配置SSL证书
使用已有的 `cert.pem` 和 `key.pem`，或使用Let's Encrypt

#### 4. 启动Nginx
```bash
cd D:\Python_project\MultiAgent\nginx
nginx.exe
```

#### 5. 配置路由器端口转发
- 登录路由器管理页面
- 添加端口转发规则：
  - 外部端口80 → 内部IP:80
  - 外部端口443 → 内部IP:443

---

## 准备工作

### 必须项
- [ ] 购买域名（方案二除外）
- [ ] 注册相关服务账号（Cloudflare/Ngrok/云服务器）

### 可选项
- [ ] 如果使用Cloudflare，修改域名DNS服务器
- [ ] 如果使用本地Nginx，配置路由器端口转发
- [ ] 如果使用云服务器，完成域名备案（国内）

### 域名注册商推荐
| 注册商 | 网址 | 价格 | 备注 |
|--------|------|------|------|
| 阿里云 | https://wanwang.aliyun.com | ¥30-60/年 | 国内访问快 |
| 腾讯云 | https://dnspod.cloud.tencent.com | ¥30-60/年 | 国内访问快 |
| Namecheap | https://www.namecheap.com | $8-10/年 | 无需备案 |
| Cloudflare | https://www.cloudflare.com | 成本价 | 自带CDN |

---

## 常见问题

### Q1: 我没有公网IP怎么办？
**A:** 使用Cloudflare Tunnel或Ngrok，无需公网IP即可实现域名访问。

### Q2: 域名需要备案吗？
**A:** 
- 国内服务器（阿里云/腾讯云）：需要备案
- Cloudflare/Ngrok/海外服务器：不需要备案

### Q3: 免费方案有限制吗？
**A:** 
- Cloudflare Tunnel：基本无限制
- Ngrok免费版：有带宽（40GB/月）和连接数限制，每次重启链接变化

### Q4: 如何保证安全？
**A:** 
- 所有方案都支持HTTPS加密传输
- 建议添加身份验证
- 限制访问IP（如可能）
- 不要将敏感项目暴露到公网

### Q5: File System Access API能在域名下使用吗？
**A:** 
- ✅ 可以，但必须使用HTTPS
- 所有推荐的方案都自动提供HTTPS支持

### Q6: 能固定域名吗？
**A:** 
- Cloudflare Tunnel：✅ 可以（需要自己的域名）
- Ngrok免费版：❌ 每次重启变化
- Ngrok付费版：✅ 可以固定
- 云服务器：✅ 可以

### Q7: 性能如何？
**A:** 
- Cloudflare Tunnel：良好（自带CDN加速）
- Ngrok：一般（有一定延迟）
- 云服务器：优秀（取决于服务器配置）

---

## 相关文件

### 项目文件
| 文件 | 说明 |
|------|------|
| `main.py` | 主程序，支持HTTPS自动检测 |
| `cert.pem` | SSL证书文件 |
| `key.pem` | SSL私钥文件 |
| `generate_cert.py` | SSL证书生成脚本 |
| `ngrok.exe` | Ngrok可执行文件 |
| `setup-domain.bat` | 域名配置引导脚本 |
| `nginx/nginx.conf` | Nginx配置文件模板 |

### 文档文件
| 文件 | 说明 |
|------|------|
| `域名访问配置指南.md` | 本文档 |
| `多机访问配置指南.md` | 局域网HTTPS配置指南 |
| `ngrok使用说明.md` | Ngrok详细使用说明 |

---

## 下一步行动

### 推荐方案：Cloudflare Tunnel

1. **购买域名**（约¥30-60/年）
2. **注册Cloudflare账号**（免费）
3. **运行配置脚本**：`setup-domain.bat`，选择选项1
4. **按照提示完成配置**

### 临时演示方案：Ngrok

1. **注册Ngrok账号**（免费）
2. **获取Authtoken**
3. **配置Authtoken**：`.\ngrok.exe config add-authtoken YOUR_TOKEN`
4. **启动Ngrok**：`.\ngrok.exe http 5001`

---

## 技术支持

如遇到问题，可参考：
- Cloudflare文档：https://developers.cloudflare.com/cloudflare-one/
- Ngrok文档：https://ngrok.com/docs
- Nginx文档：https://nginx.org/en/docs/

---

*最后更新：2026-07-25*