# 多机访问配置指南

## 已完成配置

✅ SSL证书已生成
✅ main.py已支持HTTPS自动检测
✅ 其他IP的电脑现在可以正常使用所有功能

## 使用方法

### 1. 启动服务器

```bash
python main.py
```

服务器会自动检测SSL证书并启用HTTPS。

### 2. 本机访问

- HTTP: `http://localhost:5001`
- HTTPS: `https://localhost:5001`

### 3. 其他电脑访问

假设你的电脑IP是 `192.168.1.100`，其他电脑访问：

```
https://192.168.1.100:5001
```

**重要提示：**
- 第一次访问时会显示安全警告（因为是自签名证书）
- 点击"高级" → "继续访问"或"接受风险并继续"
- 之后就可以正常使用所有功能

### 4. 防火墙配置（如无法访问）

如果其他电脑无法访问，需要开放5001端口：

**Windows防火墙设置：**
1. 打开"Windows Defender 防火墙"
2. 点击"高级设置"
3. 点击"入站规则" → "新建规则"
4. 选择"端口" → "TCP" → 输入"5001"
5. 选择"允许连接"
6. 命名规则为"MultiAgent 5001"

**或使用PowerShell命令：**
```powershell
New-NetFirewallRule -DisplayName "MultiAgent 5001" -Direction Inbound -Protocol TCP -LocalPort 5001 -Action Allow
```

## 功能可用性

| 功能 | HTTP访问 | HTTPS访问 |
|------|----------|-----------|
| 导入项目（选择文件夹） | ❌ | ✅ |
| 显示文件树 | ❌ | ✅ |
| 查看文件内容 | ❌ | ✅ |
| 编辑文件 | ❌ | ✅ |
| 运行Python代码 | ❌ | ✅ |
| AI聊天 | ✅ | ✅ |

## 重新生成证书

如果需要重新生成证书：

```bash
# 删除现有证书
del cert.pem
del key.pem

# 重新生成
python generate_cert.py
```

## 注意事项

1. **自签名证书**：浏览器会显示安全警告，这是正常的
2. **IP变化**：如果你的电脑IP变化，需要通知其他用户使用新IP
3. **证书有效期**：当前证书有效期为10年（到2036年）
4. **安全性**：这是自签名证书，仅适用于局域网内部使用